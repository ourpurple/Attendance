<!-- pages/attendance/attendance.wxml -->
<view class="container">
  <!-- 月份选择 -->
  <view class="date-filter">
    <picker mode="date" fields="month" value="{{selectedMonth}}" bindchange="onMonthChange">
      <view class="picker">
        <text>{{selectedMonth}}</text>
        <text class="picker-icon">📅</text>
      </view>
    </picker>
  </view>

  <!-- 考勤列表 -->
  <view class="attendance-list" wx:if="{{attendanceList && attendanceList.length > 0}}">
    <view class="list-item {{item.isAbsent ? 'list-item-absent' : ''}}" wx:for="{{attendanceList}}" wx:key="id">
      <view class="list-item-header">
        <text class="list-item-title">{{item.date}}</text>
        <text wx:if="{{item.workHours}}">{{item.workHours}}小时</text>
      </view>
      <view class="list-item-content">
        <view class="attendance-row">
          <text>上班: {{item.checkinTime || '-'}}</text>
          <view class="checkin-status-badge {{item.checkinStatusClass}}" wx:if="{{item.checkinStatusText}}">{{item.checkinStatusText}}</view>
          <text class="status-badge status-warning" wx:if="{{item.isLate}}">迟到</text>
        </view>
        <view class="attendance-location" wx:if="{{item.checkinLocation}}">
          <text>📍 {{item.checkinLocation}}</text>
        </view>
        <view class="attendance-row">
          <text>下班: {{item.checkoutTime || '-'}}</text>
          <view class="checkout-status-badge {{item.checkoutStatusClass}}" wx:if="{{item.checkoutStatusText}}">{{item.checkoutStatusText}}</view>
        </view>
        <view class="attendance-location" wx:if="{{item.checkoutLocation}}">
          <text>📍 {{item.checkoutLocation}}</text>
        </view>
      </view>
    </view>
  </view>

  <view class="empty-state" wx:else>
    <view class="empty-state-icon">📝</view>
    <view class="empty-state-text">本月暂无考勤记录</view>
  </view>

  <!-- 版权说明 -->
  <view class="copyright-footer">
    <text class="copyright-text">© 2025 河南新盟科教有限公司 All Rights Reserved</text>
  </view>
</view>

